filebeat-remote-state role
==========================

Create filebeat terraform remote state dynamoDB lock table

Example Playbook use
--------------------
    - hosts: servers
      roles:
         - role: filebeat-remote-state

