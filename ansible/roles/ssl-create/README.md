ssl-create role
===============

Creating a Self-Signed SSL Certificate for consul, vault, jenkins

Example Playbook use
--------------------

    - hosts: servers
      roles:
         - role: ssl-create
