vault-cluster-remote-state role
================================

Create vault terraform remote state dynamoDB lock table

Example Playbook use
--------------------
    - hosts: servers
      roles:
        - role: vault-cluster-remote-state

