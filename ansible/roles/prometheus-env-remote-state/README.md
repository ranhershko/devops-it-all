prometheus-env-remote-state role
================================

Create prometheus terraform remote state dynamoDB lock table

Example Playbook use
--------------------
    - hosts: servers
      roles:
        - role: prometheus-env-remote-state

